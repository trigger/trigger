# DEPRECATED: This project has migrated to Python 3.10+ and GitHub Actions
# This file is kept for historical reference only
# See .github/workflows/tests.yml for the current CI configuration

language: python
python:
    - "2.7"

install:
    - pip install -r requirements-dev.txt
    - pip install .

env:
    - TRIGGER_SETTINGS=tests/data/settings.py

script:
    - py.test -vv tests/
